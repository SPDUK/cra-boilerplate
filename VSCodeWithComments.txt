{
    // disable telemetry
    "telemetry.enableTelemetry": false,

    // forces emmet / snippets to always display
    "editor.quickSuggestions": {
        "other": true,
        "comments": false,
        "strings": false
    },
    // makes the suggestions easier to find
    "editor.snippetSuggestions": "top",
    "emmet.showSuggestionsAsSnippets": true,
    // makes tab autocomplete with emmet
    "emmet.triggerExpansionOnTab": true,
    // removes enter key autocomplete
    "editor.acceptSuggestionOnEnter": "off",
    // emmet settings for react
    "emmet.includeLanguages": {
        "javascript": "javascriptreact"
    },
    "emmet.syntaxProfiles": {
        "javascript": "jsx"
    },

    // formats CSS, HTML etc
    "editor.formatOnSave": true,

    // stops default formatting, uses ESLint instead for React & JS files
    "[javascript]": {
        "editor.formatOnSave": false,
    },
    "[javascriptreact]": {
        "editor.formatOnSave": false,
    },
    "eslint.autoFixOnSave": true,

    // For windows, change files.eol to \n on linux
    "files.eol": "\r\n",

    // 2 spaces when hitting tab, indentation settings
    "editor.insertSpaces": true,
    "editor.tabSize": 2,
    "editor.detectIndentation": true,

    // makes text wrap around so no horizontal scrolling is needed
    "editor.wordWrap": "on",

    // disables the minimap
    "editor.minimap.enabled": false,

    // disables the feedback button
    "workbench.statusBar.feedback.visible": false,

    // removes warning when syncing with git (pull then push)
    "git.confirmSync": false,
    "git.autofetch": true,


    // shows if ESLint is working properly in bottom right
    "eslint.alwaysShowStatus": true,

    // adds highlight around the current line being selected
    "editor.renderLineHighlight": "all",

    // makes cursor a little nicer and easier to see
    "editor.cursorBlinking": "phase",
    "editor.cursorWidth": 1,
    "workbench.colorCustomizations": {
        "editorCursor.foreground": "#ededed"
    },

    // prettier settings, for when ESLint is disabled and Prettier is enabled
    "prettier.singleQuote": true,
    "prettier.eslintIntegration": true,
    "prettier.printWidth": 80,


    // removes menu bar at the top of vscode window, press alt to enable
    "window.menuBarVisibility": "toggle",
    
    // uses git bash as default terminal in VSCode (on windows)
    "terminal.integrated.shell.windows": "C:\\Program Files\\Git\\bin\\bash.exe",


    // indentation guides
    "editor.renderIndentGuides": true,
    // theme settings & font
    "workbench.colorTheme": "Dracula",
    "editor.fontFamily": "DejaVu Sans Mono",
    "workbench.iconTheme": "vscode-icons",
    "editor.fontSize": 16,

    // just fixes for some small warnings
    "javascript.updateImportsOnFileMove.enabled": "always",
    "javascript.implicitProjectConfig.experimentalDecorators": true,
    "explorer.confirmDragAndDrop": false,
}